<!DOCTYPE html>
<html>
<head>
    <style>
        table {
            margin: 0 auto;
            width: 90%;
            border-collapse: collapse; /* 테이블 셀 경계 합치기 */
        }
        th, td {
            border: 1px solid #ddd; /* 셀 경계 */
            padding-top: 10px;
            padding-bottom: 10px;
            padding-left: 30px;
            padding-right: 30px;
            text-align: center;
        }
        th {
            background-color: #f2f2f2;
        }
        .left-align {
            text-align: left;
        }
        .before {
            color: #4b0082;
        }
        #newInquiryForm {
            box-shadow: none;
            align-items: center;
            padding: 20px; /* 폼의 내부 여백 */
            max-width: 80%; /* 폼의 최대 너비를 80%로 설정 */
            margin: 0 auto; /* 폼을 중앙에 위치시키기 */
            display: flex;
            flex-direction: column; /* 세로 방향 정렬 */
            border-radius: 0;
            border: none;
        }
        #content {
            border-radius: 15px;
            border: 1px solid #ccc;
            resize: none;
            margin-bottom: 30px;
            width: calc(80% - 110px);
            margin: 0 auto;
            padding: 15px;
            font-size: 16px;
            font-family: Arial, sans-serif;
            color: #333;
        }
        button {
            padding: 5px;
            display: block;
            margin: 0 auto;
            padding: 10px 20px;
            font-size: 16px;
            font-weight: bold;
            background-color: #D8CEF6; /* 버튼 배경색 */
            color: white; /* 버튼 텍스트 색상 */
            border: none; /* 버튼 테두리 없음 */
            border-radius: 10px; /* 버튼 모서리 둥글게 */
            cursor: pointer; /* 커서 모양 변경 */
            transition: background-color 0.3s; /* 배경색 변화에 트랜지션 적용 */
        }
    </style>
    <title>사용자 문의 확인</title>
</head>
<body>
    <h2>문의 내역</h2>
    <table>
        <thead>
            <tr>
                <th>번호</th>
                <th>내용</th>
                <th>지점명</th>
                <th>답장/상태</th>
            </tr>
        </thead>
        <tbody>
            <% inquiries.forEach(function(inquiry) { %>
                <tr>
                    <td><%= inquiry.qnaNumber %></td>
                    <td class="left-align"><%= inquiry.content %></td>
                    <td>
                        <%
                            const branch = branches.find(branch => branch.branchID === inquiry.branchID);
                            %>
                        <%= branch ? branch.branchName : 'Unknown' %>
                    <td>
                        <% if (!inquiry.answer) { %>
                            <p class="before">처리 전</p>
                        <% } else { %>
                            <%= inquiry.answer %>
                        <% } %>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>

    <h2>문의 작성</h2>
    <form id="newInquiryForm">
        <div>
        <label for="branch">지점 선택:</label>
        <select name="branchID" id="branch" required>
            <% branches.forEach(function(branch) { %>
                  <option value="<%= branch.branchID %>"><%= branch.branchName %></option>
            <% }); %>
        </select>
        </div>
        <br>
        <textarea id="content" name="content" required></textarea>
        <button type="submit">제출</button>
    </form>
</body>
</html>
<script>
document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('newInquiryForm');
    if (form) {
        form.addEventListener('submit', function(event) {
            event.preventDefault();

            const content = form.querySelector('textarea[name="content"]').value;
            const branchID = form.querySelector('select[name="branchID"]').value;

            fetch('/user/saveQuestion', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ content, branchID })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert('문의가 성공적으로 제출되었습니다!');
                    form.reset(); // 폼 리셋
                    location.reload(); // 새로고침하여 업데이트된 문의를 표시합니다.
                } else {
                    alert('문의 제출 중 오류가 발생했습니다: ' + data.message);
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('문의 제출 중 오류가 발생했습니다.');
            });
        });
    }
});
</script>

