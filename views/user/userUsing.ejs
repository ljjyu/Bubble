<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>사용 중인 기기</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .header {
            background-color: #d8cef6;
            padding: 10px;
            text-align: center;
            font-size: 20px;
            color: black;
            margin-left: 250px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .sidebar {
            width: 250px;
            background-color: #FFFFFF;
            position: fixed;
            height: 100%;
            border: 1px solid #d3d3d3;
        }
        .sidebar a {
            padding-top: 15px;
            padding-bottom: 15px;
            text-decoration: none;
            font-size: 18px;
            text-align: center;
            color: black;
            display: block;
            border-bottom: 1px solid #d3d3d3;
        }
        .sidebar a:hover {
            background-color: #d3d3d3;
            color: black;
        }
        .main-content {
            margin-left: 270px;
            padding: 20px;
        }
        .machine {
            background-color: #f0f0f0;
            border-radius: 10px;
            padding: 10px;
            margin-bottom: 20px;
            text-align: center;
            position: relative;
            width: 150px;
        }
        .machine.inactive {
            background-color: #d3d3d3;
            color: #555;
        }
        .machine span {
            display: block;
            margin: 10px 0;
        }
        .machine::after {
            content: attr(data-id);
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: rgba(255, 255, 255, 0.7);
            border-radius: 50%;
            padding: 5px 10px;
        }
    </style>
    <script>
        // 예약 정보를 업데이트하는 함수
        function updateReservationInfo() {
            // 서버로 예약 정보 요청
            fetch('/user/userUsing')
                .then(response => response.json())
                .then(data => {
                    // 받은 데이터를 이용하여 페이지 업데이트
                    // 여기에 예약 정보를 처리하는 코드 작성
                })
                .catch(error => {
                    console.error('예약 정보를 불러오는 동안 오류가 발생했습니다.', error);
                });
        }

        // 페이지 로드 후 초기화
        document.addEventListener('DOMContentLoaded', () => {
            // 초기화 작업
            updateReservationInfo();

            // 일정 시간마다 예약 정보 업데이트
            setInterval(updateReservationInfo, 60000); // 1분마다 업데이트
        });
    </script>
</head>
<body>
    <div class="header">사용 중인 기기</div>
    <div class="sidebar">
        <a href="/user/userMain">HOME</a>
        <a href="/user/userReserve">예약하기</a>
        <a href="/user/userHome">나의 예약 내역</a>
        <a href="/user/userUsing">나의 사용중 기기</a>
        <a href="#section4">공지사항</a>
        <a href="#section5">...</a>
    </div>
    <div class="main-content">
        <% if (reservations.length === 0) { %>
            <p>예약한 기기가 없습니다.</p>
        <% } else { %>
            <div class="machines-container">
                <% reservations.forEach((reservation) => { %>
                    <div class="machine <%= reservation.remainingTime ? '' : 'inactive' %>" data-id="<%= reservation.id %>">
                        <span class="course">기계 종류: <%= reservation.machineType %></span>
                        <% if (reservation.remainingTime) { %>
                            <span class="time">잔여 시간: <%= reservation.remainingTime %> 분</span>
                        <% } %>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>
</body>
</html>

